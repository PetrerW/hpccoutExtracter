import fileReader
import os

class hpccoutExctacter:
    __inputLabelsFilename = "labels.txt"
    __hpccoutFilename = "hpccout.txt"

    def extractLabelsWithValues(self):
        def createEmptyHashMap(self):
            l = fileReader
            labels = fileReader.fileReader.readLines(self.__hpccoutFilename)

            #TODO: Create an empty hash map
            #Fill it with values from read file

        def extractHpccOutput(self, labelsAndValues):
    #TODO


if __name__ == '__main__':
    l = hpccoutExctacter
    lines = l.extractLabelsWithValues("labels.txt")
    for line in lines:
        print(line)